# TypeSafe API key for the Jev System One model.
# Get one at https://typesafe.ai (early access). Without it the simulator
# falls back to the offline mock provider, which is NOT Jev.
TYPESAFE_API_KEY=sk-...

# Optional overrides
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# JEV_MODEL=jev-latest
