"""Exchange listing announcements, read by a two-round judgment tree.

This is the first opportunity picked by the "composed judgment in one second"
argument: a primary-source feed with millisecond timestamps, a known and large
price effect, and an incumbent -- the title-matching sniping bot -- that is fast
enough but cannot read. The study measures whether reading the announcement
(which token, which direction, how much) beats matching its title, with the
tape as the only judge.
"""
